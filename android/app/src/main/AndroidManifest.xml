<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- ACCESS_BACKGROUND_LOCATION is intentionally NOT declared. Walk
         tracking uses a foreground service typed `location` (started while
         the app is in the foreground via startWalk), which Android grants
         continuous location to without the background-location permission.
         No code path requests location while no FGS is running. Re-add this
         permission only when a feature genuinely needs background location
         outside an FGS (e.g. geofencing while app is closed) — and prepare
         a Play Console "prominent disclosure" form when you do. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Walk-around tracking: flutter_foreground_task runs the location
         stream in a separate Dart isolate tied to a real foreground service,
         so it survives Samsung One UI's "Freecess" freezing of the main app
         process. The service is started/stopped by AppState's
         startWalk/endWalk; idle app runs no service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets us pop the OS-level "Allow this app to run in the background?"
         dialog at startWalk via permission_handler. Without this exemption,
         Samsung One UI / aggressive OEM battery savers can still freeze the
         FGS isolate even with the FGS notification visible. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Hardware features declared explicitly as NOT required so Google Play
         does not filter the app off devices whose system feature manifest is
         incomplete. Without these lines, Play infers `android.hardware.camera`
         and `android.hardware.camera.autofocus` from CAMERA, and
         `android.hardware.location` + `android.hardware.location.gps` from
         ACCESS_FINE_LOCATION, all as required=true — and silently hides the
         listing on devices like the Blackview Active 8 Pro that have the
         hardware but don't declare every sub-feature in /system/etc/permissions.
         The app already degrades gracefully if a feature is genuinely absent
         (camera button is hidden, GPS-dependent UI shows a message), so
         marking these optional only widens device coverage. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <application
        android:label="${appLabel}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_foreground_task: runs the walk-tracking isolate in a
             location-typed foreground service. RestartReceiver re-starts
             the service on system events (boot, app updates) when the
             plugin's auto-restart is enabled. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />
        <receiver
            android:name="com.pravera.flutter_foreground_task.service.RestartReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
